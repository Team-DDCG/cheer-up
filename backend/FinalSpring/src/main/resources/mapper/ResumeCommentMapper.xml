<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="ResumeCommentMapper">

	<insert id="registerResumeComment" parameterType="resumeCommentVO">
		INSERT
		INTO resume_comment(comment_id, content, rate)
		VALUES(RESUME_COMMENT_SEQ.NEXTVAL, #{content},#{rate})	
	</insert>
	
	<delete id="deleteResumeComment" parameterType="int">
		DELETE FROM resume WHERE comment_id = #{VALUE}
	</delete>
	
	<update id="updateResumeComment" parameterType="resumeCommentVO">
		UPDATE resume_comment SET
		content = #{content},
		rate = #{rate}
		
		WHERE comment_id = #{commentId}
	</update>

</mapper>