<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 
	CRUD를 다 완벽하게 구현
	C : INSERT
	R : SELECT
	U : UPDATE
	D : DELETE
 -->

<mapper namespace="UserProjectMapperr">
	<insert id="registerUserProject" parameterType="userProjectVO">
		INSERT
		INTO user_project_info(project_id, name, host_name, content, skill, agency)
		VALUES(user_project_seq.NEXTVAL,#{name}, #{hostName}, #{content}, #{skill}, #{agency})
	</insert>
	
	<delete id="deleteUserProject" parameterType="int">
		DELETE FROM user_project_info WHERE project_id=#{VALUE}
	</delete>
	
	<update id="updateUserProject" parameterType="userProjectVO">
		UPDATE user_project_info SET
		name = #{name},
		host_name = #{hostName},
		content = #{content},
		skill = #{skill},
		agency = #{agency}	
	</update>
	
	<select id="getUserProject" parameterType="int" resultType="userProjectVO">
		SELECT
		name, host_name, content, skill, agency
		FROM user_project_info
		WHERE id=#{VALUE}
	</select>
	
	
</mapper>
